version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18.x
    commands:
      - npm install -g yarn
      - cd web-scraper/
  build:
    commands:
      - docker login -u edgarpdev -p $DOCKER_CREDENTIALS
