version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18.x
  build:
    commands:
      - cd web-scraper/
      - docker login -u edgarpdev -p $DOCKER_CREDENTIALS
      - ./build-docker-codebuild.sh
